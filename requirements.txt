# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.37.0
plotly>=5.22.0          # charts on the dashboard page

# ── Data / storage ────────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0           # CSV export, DataFrame manipulation

# ── ML / model inference ──────────────────────────────────────────────────────
torch>=2.2.0
transformers>=4.45.0    # HuggingFace model wrappers (4.45+ for AutoModelForImageTextToText)
datasets>=2.19.0        # HuggingFace benchmark datasets (SQuAD, etc.)
accelerate>=0.30.0      # multi-device inference helpers
matplotlib>=3.8.0       # TS→image plotting for QwenVLModel
qwen-vl-utils>=0.0.8   # vision-info preprocessing for Qwen VL models
torchvision>=0.17.0    # required by Qwen3VLVideoProcessor

# ── Metrics ───────────────────────────────────────────────────────────────────
scikit-learn>=1.4.0     # F1 score; MSE/MAE also available as fallback
scipy>=1.11.0           # ARFF loading for the UCR ICL benchmark

# ── Dev / quality ─────────────────────────────────────────────────────────────
pytest>=8.2.0
ruff>=0.4.0
mypy>=1.10.0
